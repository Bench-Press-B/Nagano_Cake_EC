<div class='container px-5 px-sm-0'>
  <div class='row'>
    <div class="col-12">
  <h3>ショッピングカート</h3>

  <% if @cart_items.present? %>
    <%= link_to "カートを空にする", cart_items_path, method: :delete, data: {confirm: 'カートを空にしてよろしいですか？'}, class: "btn btn-danger mb-2" %>
    <table class="table table-hover table-bordered">
    <tr>
      <th>商品名</th>
      <th>単価(税込)</th>
      <th>数量</th>
      <th>小計</th>
      <th></th>
    </tr>
    <% @cart_items.each do |i| %>
    <% @total_price = (@total_price+ i.subtotal) %>
      <tr id="cart-<%= i.id %>">
        <th>
          <%= image_tag i.item.get_image, size: "220x120" %>
          <%= i.item.name %>
        </th>
        <th><%= (i.item.non_taxed_price * 1.1).floor.to_s(:delimited, delimiter: ',') %></th>
        <th>
          <%= form_with model:i, url: cart_item_path(i), method: :patch  do |f| %>
          <%= f.number_field :quantity, size: 10, min: 1 %>
          <%= f.submit "変更", class: "btn btn-success" %>
          <% end %>
        </th>
        <th><%= i.subtotal.floor.to_s(:delimited, delimiter: ',') %></th>
        <th>
          <%= link_to " 削除する", cart_item_path(i), method: :delete, data: {confirm: "#{i.item.name}を削除してよろしいですか？"}, class: "btn btn-danger" %>
        </th>
      </tr>
    <% end %>
    </table>

    <%= link_to " 買い物を続ける", root_path, class: "btn btn-info mb-3" %>

    <table class="table table-bordered text-right">
    <tr>
      <td>合計金額</td>
      <td id='total_price'>
        ¥<%= @total_price.floor.to_s(:delimited, delimiter: ',') %>
      </td>
    </tr>
    </table>
    <%= link_to " 情報入力に進む", new_order_path, class: "btn btn-success center-block" %>
  <% else %>
    no cart
  <% end %>
    </div>
</div>
</div>
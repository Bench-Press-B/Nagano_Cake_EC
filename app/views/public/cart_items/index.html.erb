<div class='container px-5 px-sm-0'>
  <div class='row'>
    <div class="col-12">
  <h3><i class="fa-solid fa-cart-shopping"></i>ショッピングカート</h3>

  <% if @cart_items.present? %>
    <%= link_to "カートを空にする", cart_items_path, method: :delete, data: {confirm: 'カートを空にしてよろしいですか？'}, class: "btn btn-danger mb-2" %>
    <table class="table table-hover table-bordered">
    <tr>
      <th>商品名</th>
      <th>単価(税込)</th>
      <th>数量</th>
      <th>小計</th>
      <th></th>
    </tr>
    <% @cart_items.each do |i| %>
      <tr id="cart-<%= i.id %>">
        <%= render "items_form", cart_item: i %>
      </tr>
    <% end %>
    </table>

    <%= link_to " 買い物を続ける", root_path, class: "btn btn-info mb-3" %>

    <table class="table table-bordered text-right">
    <tr>
      <td>合計金額</td>
      <td id='total_price'>
        ¥<%= total_price(@cart_items).to_s(:delimited) %>
      </td>
    </tr>
    </table>
    <%= link_to " 情報入力に進む", new_order_path, class: "btn btn-success center-block" %>
  <% else %>
    <p>お客様のショッピングカートに商品はありません。</p>
    <p><%= link_to "商品一覧", items_path, class: "btn-link" %>から商品をお探し下さい。</p>
  <% end %>
    </div>
</div>
</div>
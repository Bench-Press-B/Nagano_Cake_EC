<div class='container px-5 px-sm-0'>
  <div class='row'>
    <div class='col-md-2'>
     ジャンル検索欄
    </div>
    <div class='col-md-10'>
      <div class="d-flex flex-row">
        <div><%= image_tag @item.get_image, size: "400x300" %></div>
        <div class="flex-column">
          <h4><%= @item.name %></h4>
          <p><%= @item.explanation %></p>
          <p>¥<%= (@item.non_taxed_price * 1.1).floor.to_s(:delimited) %>(税込)</p>
          <div class="flex-row">
            <%= form_with model:@cart_items, url:cart_item_path() %>
          </div>
        </div>
        
      </div>
      </div>
    </div>
  </div>
</div>